// The harness compiles the app's data + retrieval sources directly (see the
// csproj), so it needs the same project-wide usings the app itself declares.
global using dinospace;
global using dinospace.Models;
global using dinospace.Data;
global using dinospace.Services;
