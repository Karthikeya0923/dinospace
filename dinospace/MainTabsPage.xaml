<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="dinospace.MainTabsPage"
             Shell.NavBarIsVisible="False"
             Title="DinoSpace">
    <Grid RowDefinitions="*,Auto" BackgroundColor="#101014">

        <CarouselView x:Name="Pager" Grid.Row="0"
                      Loop="False"
                      IsSwipeEnabled="True"
                      IsBounceEnabled="False"
                      PeekAreaInsets="0"
                      PositionChanged="OnPositionChanged"
                      HorizontalScrollBarVisibility="Never"
                      VerticalScrollBarVisibility="Never">
            <CarouselView.ItemTemplate>
                <DataTemplate>
                    <ContentView Content="{Binding}"/>
                </DataTemplate>
            </CarouselView.ItemTemplate>
        </CarouselView>

        <VerticalStackLayout Grid.Row="1" Spacing="0" BackgroundColor="#15151B">
            <Grid x:Name="TabBar" ColumnDefinitions="*,*,*,*" Padding="0,10" ColumnSpacing="0"/>
        </VerticalStackLayout>

    </Grid>
</ContentPage>